# 班达 UI 设计规范

## 目标

界面以“快速找到、直接操作、状态清楚”为优先级。教师端和教室端使用同一视觉语言，不使用大面积渐变、毛玻璃、装饰性高光或无意义动画。

## 设计令牌

| 用途 | 值 |
| --- | --- |
| 页面背景 | `#F6F7F9` |
| 内容面 | `#FFFFFF` |
| 主文字 | `#172033` |
| 次要文字 | `#667085` |
| 弱提示 | `#98A2B3` |
| 分隔线 | `#E4E7EC` |
| 主操作 | `#2563EB` |
| 成功 | `#16A34A` |
| 警告 | `#D97706` |
| 危险 | `#DC2626` |
| 常用圆角 | `8px` / `10px` / `12px` |

教师端覆盖样式位于 `teacher-app/ui.css`，教室端覆盖样式位于 `classroom-app/ui.css`。新页面应优先复用这些令牌和现有组件。

## 使用规则

1. 每个区域只保留一个蓝色主按钮；取消、刷新、导入等操作使用白色次按钮。
2. 删除、清空、拒绝等不可逆操作使用红色文字或边框，并要求二次确认。
3. Emoji 不承担导航、状态或按钮含义；需要图标时使用同一套矢量图标并保留文字标签。
4. 普通内容面只使用细边框和一级浅阴影；弹窗才使用较强阴影。
5. 表单控件高度不低于 `36px`，标签位于控件上方，错误信息紧邻对应控件。
6. 顶部标签使用文字和底部蓝线表达选中状态，同时同步 `aria-selected`。
7. 弹窗必须支持 Escape 关闭、限制最大高度，并在小窗口内滚动。
8. 动态状态使用 `aria-live`，危险或阻断错误使用 `role="alert"`。
9. 520px 宽的管理窗口允许宽表格横向滚动，不压缩到无法阅读。
10. 新增样式必须在实际 Electron 窗口尺寸和最小窗口尺寸下分别检查。

## 页面尺寸基线

| 页面 | 验收尺寸 |
| --- | --- |
| 教师端 | `1120×770`，最小 `920×620` |
| 教室管理 | `720×620`，最小 `520×420` |
| 作业看板 | `700×480` |
| 呼叫通知 | `500×300` |
| 人脸注册 | `520×580` |
| 密码验证 | `380×280` |

## 教师端账户流程

1. 每次启动先显示教师登录，主界面在登录完成前保持不可交互。
2. 首次使用先创建本机教师账户；账户密码只在教师电脑本地校验，不发送到局域网。
3. 登录后才显示教室连接入口和已保存教室。
4. 第一次连接某间教室时自动提交教师身份，页面进入“等待审核”。
5. 教室管理员批准后，当前连接自动进入呼叫主页；拒绝或移除后立即停止同步教室数据。
6. 账户管理入口固定在教师端侧栏底部，退出登录后保留账户和历史，但必须重新输入密码才能继续使用。
