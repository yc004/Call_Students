# Electron 学生呼叫系统 + 人脸识别签到 + 自适应优化 — 完整设计文档

## 1. 项目概述
在已有的 Electron 学生呼叫系统中增加**人脸识别签到功能**，实现：
- 自动检测教室摄像头画面中的学生人脸。
- 识别学生身份（与预注册底库比对）。
- 识别结果实时反馈到呼叫系统界面（标记出勤）。
- **自适应在线优化**：系统在使用中自动收集高置信度的人脸特征，动态更新底库，使识别越来越准。

## 2. 技术栈
- **框架**：Electron (主进程 Node.js + 渲染进程 Chromium)
- **人脸识别库**：`face-api.js` (基于 TensorFlow.js，纯 JS，不依赖 Python)
- **数据存储**：本地 JSON 文件 (`gallery.json`)
- **通信**：IPC (contextBridge + ipcMain/ipcRenderer)

## 3. 功能模块划分
```
项目根目录/
├── main/                  # Electron 主进程
│   ├── main.ts
│   ├── adaptiveGallery.ts # 自适应底库管理
│   └── recognition.ts     # 人脸识别服务
├── preload/
│   └── preload.ts
├── renderer/              # 渲染进程 (你的呼叫系统 UI)
│   ├── index.html
│   └── renderer.js
├── models/                # face-api.js 的预训练模型文件
├── data/
│   └── gallery.json       # 底库数据
└── package.json
```

## 4. 人脸识别库集成方案

### 4.1 库的选择：face-api.js
- 提供 SSD Mobilenet V1 检测器（轻量、快）
- 提供 68 点关键点模型
- 提供 FaceNet 128 维特征提取
- 所有模型文件 < 10MB，可打包进应用

### 4.2 模型文件下载
从 [face-api.js 官方仓库](https://github.com/justadudewhohacks/face-api.js/tree/master/weights) 下载以下文件，放入 `models/` 目录：
- `ssd_mobilenetv1_model-shard1`
- `ssd_mobilenetv1_model-shard2`
- `face_landmark_68_model-shard1`
- `face_landmark_68_model-shard2`
- `face_recognition_model-shard1`
- `face_recognition_model-shard2`
- 对应的 `.manifest.json` 文件

### 4.3 在 Electron 中加载模型
在 `main.ts` 启动时加载模型（仅加载一次）：
```typescript
import * as faceapi from 'face-api.js';
import path from 'path';

const MODEL_PATH = path.join(__dirname, '..', 'models');

async function loadModels() {
  await faceapi.nets.ssdMobilenetv1.loadFromDisk(MODEL_PATH);
  await faceapi.nets.faceLandmark68Net.loadFromDisk(MODEL_PATH);
  await faceapi.nets.faceRecognitionNet.loadFromDisk(MODEL_PATH);
}
```

## 5. 自适应底库管理器 (AdaptiveGalleryManager)

### 5.1 数据文件 `gallery.json` 结构
```json
{
  "students": [
    {
      "id": "zhangsan",
      "name": "张三",
      "registeredDescriptors": [
        [0.12, -0.34, ...]   // 128维特征数组
      ],
      "adaptiveDescriptors": [],
      "lastUpdated": "2025-06-20T10:00:00Z"
    }
  ],
  "config": {
    "maxAdaptiveSamples": 5,
    "additionThreshold": 0.85,
    "expiryDays": 30
  }
}
```

### 5.2 类接口设计（TypeScript）
```typescript
class AdaptiveGalleryManager {
  private students: Map<string, StudentRecord>;
  private config: Config;
  private filePath: string;

  constructor(filePath: string);
  async load(): Promise<void>;           // 从 JSON 加载
  async save(): Promise<void>;           // 持久化

  // 核心功能
  getAllStudentIds(): string[];
  getStudentName(id: string): string;
  getDescriptors(id: string): Float32Array[];   // 注册 + 自适应特征
  computeBestSimilarity(id: string, descriptor: Float32Array): number;

  // 自适应更新
  tryAddAdaptiveDescriptor(id: string, descriptor: Float32Array, similarity: number): boolean;
  cleanExpired(): void;                 // 删除过期自适应特征
  resetAdaptive(id: string): void;      // 重置某学生的自适应特征
}
```

### 5.3 关键算法细节
- **相似度计算**：使用余弦相似度 (cosineSimilarity = 1 - a.dot(b) / (norm(a)*norm(b))，或直接使用 `faceapi.euclideanDistance` 取倒数，这里统一用 **余弦相似度**，范围 [0,1]。
- **识别判定**：若与某学生的最大相似度 ≥ 0.6，则认定为此人；否则为 unknown。
- **入库条件**：当识别成功且相似度 ≥ 0.85，且该学生的自适应特征数量未满，则将此特征加入 adaptiveDescriptors。
- **容量控制**：maxAdaptiveSamples=5，超出时移除最早加入的特征（FIFO）。
- **过期清理**：超过 30 天未更新的自适应特征自动删除，但保留注册特征。
- **序列化**：`Float32Array` 转为普通数组存储，加载时转回。

## 6. 识别服务 (RecognitionService)

### 6.1 核心流程
```typescript
class RecognitionService {
  private gallery: AdaptiveGalleryManager;

  constructor(gallery: AdaptiveGalleryManager);

  async recognize(imageBuffer: Buffer): Promise<RecognizedStudent[]> {
    // 1. 解码图像
    const img = await canvas.loadImage(imageBuffer);
    // 2. 检测所有人脸 + 关键点 + 描述符
    const detections = await faceapi.detectAllFaces(img)
      .withFaceLandmarks()
      .withFaceDescriptors();
    
    const results: RecognizedStudent[] = [];
    for (const det of detections) {
      const desc = det.descriptor;
      let bestId = null;
      let bestSim = 0;
      // 3. 遍历底库所有学生
      for (const id of this.gallery.getAllStudentIds()) {
        const sim = this.gallery.computeBestSimilarity(id, desc);
        if (sim > bestSim) {
          bestSim = sim;
          bestId = id;
        }
      }
      // 4. 判断身份
      if (bestSim >= 0.6 && bestId) {
        results.push({ id: bestId, name: this.gallery.getStudentName(bestId), similarity: bestSim });
        // 5. 尝试自适应优化
        this.gallery.tryAddAdaptiveDescriptor(bestId, desc, bestSim);
      } else {
        results.push({ id: 'unknown', name: '未知', similarity: bestSim });
      }
    }
    // 6. 异步保存底库（可节流）
    await this.gallery.save();
    return results;
  }
}
```

## 7. 学生注册流程（提前录入底库）
在系统首次使用或新增学生时，需要录入人脸特征。提供一个简单的注册界面（渲染进程）：
1. 打开摄像头，捕获学生正面照片。
2. 调用主进程 `register-face` IPC，传入图像和 studentId。
3. 主进程检测人脸，提取描述符，存入 `registeredDescriptors`。
4. 持久化保存。

**主进程注册接口**：
```typescript
ipcMain.handle('register-face', async (event, studentId: string, name: string, imageBase64: string) => {
  const buffer = Buffer.from(imageBase64, 'base64');
  const img = await canvas.loadImage(buffer);
  const detections = await faceapi.detectSingleFace(img).withFaceLandmarks().withFaceDescriptor();
  if (!detections) throw new Error('未检测到人脸');
  // 添加到 gallery
  gallery.addStudent(studentId, name, [detections.descriptor]);
  await gallery.save();
  return { success: true };
});
```

## 8. IPC 通信设计
### 8.1 预加载脚本暴露 API
```typescript
contextBridge.exposeInMainWorld('faceAPI', {
  recognize: (imageBase64: string) => ipcRenderer.invoke('recognize-face', imageBase64),
  register: (studentId: string, name: string, imageBase64: string) =>
    ipcRenderer.invoke('register-face', studentId, name, imageBase64),
  getStudents: () => ipcRenderer.invoke('get-students'),
  resetAdaptive: (studentId: string) => ipcRenderer.invoke('reset-adaptive', studentId),
});
```

### 8.2 主进程注册 ipcMain handles
```typescript
ipcMain.handle('recognize-face', async (_, imageBase64) => {
  const buffer = Buffer.from(imageBase64, 'base64');
  return await recService.recognize(buffer);
});
// ... 其他 handle 同上
```

## 9. 渲染进程调用示例
```javascript
// 定时抓取摄像头帧并识别（每秒一次）
setInterval(async () => {
  const video = document.getElementById('video');
  const canvas = document.createElement('canvas');
  canvas.width = 320;   // 低分辨率，加速
  canvas.height = 240;
  canvas.getContext('2d').drawImage(video, 0, 0, 320, 240);
  const base64 = canvas.toDataURL('image/jpeg', 0.6).split(',')[1];

  const results = await window.faceAPI.recognize(base64);
  updateStudentList(results);   // 更新 UI 中的出勤状态
}, 1000);
```

## 10. 性能优化要点
- **图像尺寸**：识别前将图像缩放到 320x240，极大减少计算量。
- **推理频率**：每秒只识别一次，避免 CPU 占用过高。
- **模型加载**：应用启动时加载一次，常驻内存。
- **底库保存节流**：设置 `save` 方法在 30 秒内最多写入一次，避免频繁 I/O。

## 11. 自适应优化评估实验（答辩用）
设计对比实验：关闭自适应功能的静态底库 vs 启用自适应功能的动态底库，在连续几天的测试数据上统计识别准确率。可生成图表展示准确率提升趋势，证明在线优化的有效性。

## 12. 实施步骤 (由 Claude Code 执行)
1. **创建 `models/` 目录，并确保模型文件已下载。**
2. **实现 `AdaptiveGalleryManager` 类** (`src/main/adaptiveGallery.ts`)，包含加载、保存、特征管理、过期清理。
3. **实现 `RecognitionService`** (`src/main/recognition.ts`)，依赖上述类，集成 face-api.js 检测与识别。
4. **在主进程入口 (`main.ts`) 中**：
   - 加载模型
   - 初始化 `AdaptiveGalleryManager`
   - 初始化 `RecognitionService`
   - 注册所有 IPC handles
5. **编写 `preload.ts`** 暴露 API。
6. **在渲染进程添加注册界面**（简单的摄像头拍照 + 姓名输入框，调用 `register-face`）。
7. **在主界面集成定时识别**（调用 `recognize-face`，更新学生列表）。
8. **添加设置界面**：可调整阈值、查看底库状态、重置自适应特征。
9. **编写测试脚本**（可选）：模拟连续多日数据，验证自适应优化效果。

## 13. 注意事项
- `face-api.js` 内部使用 `@tensorflow/tfjs-node` ？为保持轻量和纯前端，建议使用 `face-api.js` 的浏览器版本（webgl backend），在 Electron 中直接跑在渲染进程或主进程的 web 环境中，无需 tfjs-node。具体做法：在主进程或渲染进程加载 face-api.js，并使用 `tf.setBackend('webgl')` 加速。
- 若想在主进程中使用，需加载 `@tensorflow/tfjs-node-gpu` ？不建议，太重。可以将识别逻辑放在**渲染进程的 web worker** 中，或直接在主进程用 `face-api.js` + `canvas` 模拟 (node-canvas)。为简化，**建议识别逻辑运行在渲染进程**，通过 Web Worker 避免卡顿。但如果已有主进程调用，可以用 `face-api.js` + `canvas` (node-canvas 包) 实现，性能足够。

**推荐架构简化**：将识别全部放在**渲染进程**，利用 WebGL 加速，不经过主进程。只需在 preload 中暴露自适应底库的读取/保存接口给渲染进程，底库文件由主进程管理。这样避免了大量图像数据通过 IPC 传递。设计文档稍作调整：识别在渲染进程，底库管理由主进程提供 IPC 读写。

但为了统一，我们可以保留主进程识别的设计，因为图像数据 base64 大小可控（320x240 约 15KB），IPC 开销极小，实现简单。

