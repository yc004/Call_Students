# 班达微信小程序设计方案

## 产品定位

小程序是教师端的随身遥控器，不替代桌面教师端的完整班级配置和人脸管理。移动端只保留适合短时、单手完成的高频操作：呼叫学生、查看与布置作业、切换教室。

## 信息结构

| 一级入口 | 主要任务 | 设计原则 |
| --- | --- | --- |
| 呼叫 | 搜索学生、选择固定呼叫话术、发送呼叫 | 首页直接操作，最少两次点击完成 |
| 作业 | 按日期/学科查看作业、布置本科作业 | 同一学科合并展示，布置使用底部面板 |
| 我的 | 查看身份、切换或添加教室、退出登录 | 低频设置集中放置 |

班主任和任课教师共用同一套界面，功能由教室端下发的角色和科目授权决定。任课教师只能为获授权科目布置作业；班主任可以使用教室端返回的全部班级学科。

## 登录与会话

1. 教师先在电脑教师端完成账户登录。
2. 在“账户管理 → 微信小程序登录”生成二维码。
3. 小程序只接受 `CLASSROOM-CALL-MINI-1` 格式的二维码，导入教师身份及最多 8 个已保存教室。
4. 凭证保存在微信本地存储，之后启动不再要求扫码。
5. 小程序进入前台后自动连接当前教室；网络断开时自动重连。
6. 主动退出登录才会清除本地凭证。

微信会暂停后台小程序，因此“一直在线”的准确含义是“一次登录后长期保持登录，回到前台自动恢复局域网连接”，不是在手机后台永久保持 WebSocket。

## 连接状态

- `offline`：显示网络诊断提示和重新连接按钮；
- `connecting`：展示正在连接或验证身份；
- `waiting`：教师身份尚未被班主任批准；
- `online`：开放呼叫与作业操作；
- 权限错误：保留页面状态，以轻提示说明具体原因。

## 视觉规范

- 主色 `#2563EB`，正文 `#172033`，背景 `#F5F7FA`；
- 内容卡片采用白色、细边框和轻阴影，不使用大面积渐变和玻璃效果；
- 底部固定三入口，核心触控区域不小于 84rpx；
- 只有当前页面和主要提交按钮使用蓝色；
- 成功、等待、离线分别使用绿、橙、灰状态点；
- 列表以整行触控为主，避免手机端密集小按钮。

## 后续安全升级

当前二维码复用桌面端长期登录密钥，适合纯局域网首版。正式大规模部署前建议升级为：教师端生成 2 分钟有效的一次性配对码，手机交换独立设备令牌，教师端可查看并吊销已登录手机。这样二维码截图不会成为永久凭证。
