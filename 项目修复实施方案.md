# 班达修复实施方案

## 当前执行状态（2026-08-11）

用户已指定优先完整修复“原生构建与模型发布链路”，因此阶段 1 已先行完成实现：

- 原生识别链路已固定为 YuNet 2023mar 检测/五点定位、112×112 相似变换对齐、SFace 2021dec 生成 128 维归一化特征。
- 模型通过 `models/onnx/models.json` 固定来源、大小与 SHA-256；模型二进制不进入 Git，可由 `npm run prepare:models` 重建。
- CMake/Electron N-API 构建、ONNX Runtime 动态库复制、打包资源路径和发布后资源校验已接通。
- 原生测试不可再因插件或模型缺失而跳过；本机已通过模型加载、推理、匹配、输入边界和图库迁移测试。
- 图库新增 `embeddingModel` 隔离。发现旧 `face-api.js` 图库时，先生成时间戳备份，再建立 SFace 空图库，并提示重新录入。
- GitHub Actions 已配置 Windows、macOS、Linux 三平台构建与发布门禁；当前只在 macOS arm64 本机完成了实际打包验证，其他两个平台需由 CI 执行确认。

原生链路的操作与发布说明见 `classroom-app/native/README.md`。阶段 2 及之后的认证、隐私、数据一致性问题尚未因此自动完成，仍须按本文顺序继续。

## 1. 文档目的与原则

本文把当前已确认的问题转化为可执行的修复计划，覆盖教师端、教室端、WebSocket 通信、人脸识别、构建发布和测试。它不直接替代功能需求文档；任何涉及产品策略的变更均应先由负责人确认。

修复遵循以下顺序：

1. **先止损**：关闭未授权访问和隐私泄露路径。
2. **再恢复交付**：让全新环境可以确定地构建、安装和启动。
3. **后提升可靠性**：处理数据一致性、性能、可观测性和测试。
4. 每一阶段都要先通过验收再进入下一阶段，避免在不可构建或不安全的基础上叠加功能。

本次实施不迁移既有学生、作业和考勤数据。由于 SFace 特征与旧 `face-api.js` 特征不兼容，人脸图库采用“保留完整备份、活动库清空、人工重新录入”的升级策略；涉及数据库结构时必须先做可恢复备份。

## 2. 修复范围、优先级与依赖关系

| 阶段 | 优先级 | 目标 | 依赖 | 完成标准 |
| --- | --- | --- | --- | --- |
| 0 | P0 | 建立基线、备份和回归环境 | 无 | 可重现当前版本与测试结果 |
| 1 | P0 | 修复原生模块构建和发布链路 | 阶段 0 | 三个平台至少能构建；原生模块可明确启用或降级 |
| 2 | P0 | 重建教师身份认证与 WebSocket 授权 | 阶段 0 | 未授权连接不能读取、呼叫或修改任何数据 |
| 3 | P0 | 收紧人脸数据处理和传输 | 阶段 2 | 人脸图像/特征不再向普通连接广播 |
| 4 | P1 | 修复数据写入、并发和服务稳定性 | 阶段 1、2 | 数据更新原子、服务异常可恢复 |
| 5 | P1 | 优化人脸识别性能与正确性 | 阶段 1、3 | 有基准、限流和准确率验证 |
| 6 | P1 | 补齐自动化测试、CI 和发布文档 | 阶段 1–5 | CI 通过且发布包可安装验证 |

## 3. 阶段 0：冻结基线与保护数据

### 要修改的内容

- 在仓库中新增 `docs/` 或 `test/` 下的测试说明、测试数据约定和发布检查清单。
- 在教室端启动前或提供管理页操作，对 `data.db`、`data.db-wal`、`gallery.json` 进行时间戳备份；备份完成后再执行数据库迁移。
- 将运行时数据目录、原生构建目录、模型目录的来源与用途写入 README。

### 修复方式

1. 使用 `npm ci` 代替 CI 中的 `npm install`，严格按 lockfile 安装。
2. 增加 `npm run verify`，统一执行语法检查、单元测试、原生测试与打包前检查。
3. 固定一组匿名化测试数据：学生、作业、考勤和若干无真实身份信息的人脸样本。真实学生信息不得进入 Git。
4. 对所有数据库迁移使用 SQLite 事务；迁移失败时保留原库并中止启动，不允许吞掉迁移错误后继续运行。

### 验收标准

- 空环境执行安装与验证命令能得到稳定结果。
- 迁移前后可恢复到原始数据副本。
- `git status` 不出现运行时数据库、日志、模型临时目录或构建产物。

## 4. 阶段 1：恢复原生构建与发布

### 4.1 当前问题

- `classroom-app/package.json` 在应用根目录执行 `cmake-js build`，而 `CMakeLists.txt` 位于 `native/`，构建会立即失败。
- CMake 未配置 `node-addon-api` 的 include 路径、Node addon 的输出后缀及跨平台运行时库处理。
- 应用代码依赖 `models/onnx/*.onnx`，但仓库和 CI 都未提供这三个模型。
- 原生测试在插件不存在时以成功退出，无法代表原生能力已通过验证。

### 4.2 实施选择

先由负责人确认以下二选一，未确认前不要继续扩展 C++ 代码：

| 方案 | 建议 | 内容 |
| --- | --- | --- |
| A. 暂停原生加速 | **推荐作为近期方案** | 只保留 face-api.js 路径，移除原生构建、ONNX 下载和无效 UI 状态；先交付安全、可测的系统。 |
| B. 完整交付原生加速 | 仅在有性能指标时采用 | 将 ONNX 模型、构建、动态库、签名、测试和模型一致性验证做成受 CI 约束的完整产物。 |

### 4.3 方案 A 的修改步骤

1. 删除或禁用 `build:native`、`test:native` 与原生加载分支，保留清晰的功能开关但默认关闭。
2. 从 CI 移除 ONNX Runtime 下载及 `cmake-js` 步骤。
3. `face-check.js` 只走已随应用打包的 face-api.js 模型，并在模型加载失败时显示可操作错误状态。
4. 文档中声明目前不提供原生推理，避免“已加速”的误导。

### 4.4 方案 B 的修改步骤

1. 将脚本改为 `cmake-js build --directory native`（或将顶层 CMake 文件明确转发到 `native/`），`clean` 同步调整。
2. 在 CMake 中获取 `node-addon-api` include 路径，设置 `PREFIX ""`、`SUFFIX ".node"`，并确保目标名称与 `face_native_addon.node` 一致。
3. 把 ONNX 模型作为可追溯构建输入：使用受校验的下载地址和 SHA-256，或将经许可的模型放在受控制品仓库。不要在 CI 临时转换未锁定的 TensorFlow 模型。
4. 每个平台将 ONNX Runtime 动态库复制到 `.node` 同目录，并验证 Windows DLL、macOS dylib 的 rpath、Linux so 的 rpath。
5. 原生测试必须在插件或模型缺失时退出非零；将“可选跳过”改为显式 `test:native:smoke`，生产 CI 使用不允许跳过的 `test:native`。
6. 使用同一批匿名人脸样本比较 JS 与原生描述符、检测框和识别结果，预先定义可接受误差与准确率阈值。

### 验收标准

- Windows、macOS、Linux 的 CI 均能从干净 checkout 完成构建。
- 成品启动后明确记录当前使用 JS 或 Native 引擎。
- Native 模式下成功加载三个模型并通过不可跳过的冒烟测试；失败时自动切换 JS 路径且提示原因。

## 5. 阶段 2：重建身份认证和 WebSocket 授权

### 5.1 安全目标

教室端不能把“能够连上端口”视为教师身份。默认拒绝所有业务请求，只有已完成身份认证、经教室管理员批准且会话有效的教师，才能读取与其职责匹配的数据或执行操作。

### 5.2 协议设计

建议将 WebSocket 协议升级为版本化协议（例如 `protocolVersion: 2`），移除“无 `connectionId` 即班主任”的兼容后门。

```text
教师端 -> hello { protocolVersion, deviceId, teacherId, nonce }
教室端 -> challenge { nonce, expiresAt }
教师端 -> authenticate { teacherId, deviceId, proof }
教室端 -> session { accessToken, permissions, expiresAt }
教师端 -> 业务消息 { type, requestId, accessToken, payload }
```

`proof` 不应是明文密码或普通 SHA-256。较简单的首版可采用：管理员为每位教师生成一次性注册码；教师端以 OS 安全存储保存随机设备密钥；教室端保存密钥的 Argon2id/scrypt 哈希。更成熟的方案可使用短期签名令牌和设备密钥轮换。

### 5.3 具体修改

1. 教师端新增首次注册/待审批界面，生成稳定 `deviceId`，并在每次连接发送身份材料。
2. 教室端仅将经批准的 `teacherId + deviceId` 绑定为可认证身份；不可让客户端上报角色或学科决定权限。
3. 删除 `main.js` 中旧版连接自动赋予“班主任”的分支；旧客户端需升级或由管理员显式迁移。
4. 为每个 WebSocket 会话设置 `authenticated`、`permissions`、`expiresAt`，在所有消息分发前做统一校验。
5. `call`、`update-submission`、`update-assignments`、`update-subjects`、`label-face` 必须分别检查授权；默认拒绝未知 `type`。
6. 仅在获授权后发送最小必要的同步数据。授课教师只能看到获分配学科，不能默认获得全班人脸和管理信息。
7. 限制连接数、每连接消息尺寸、消息频率和认证失败次数；记录来源地址、身份、请求 ID、拒绝原因，但不记录密码、令牌或人脸特征。
8. 若使用跨网段或 Wi-Fi，使用 `wss` 和受信任证书；仅局域网也应提供 TLS 配置并默认不监听公共网络接口。至少支持绑定指定网卡/IP。

### 验收标准

- 未认证、待审批、过期会话、伪造角色、伪造 `teacherId` 都无法读取 `sync` 或触发任何业务效果。
- 班主任与授课教师的权限矩阵有自动化测试。
- 认证/令牌/异常消息不会以明文出现在日志或磁盘。

## 6. 阶段 3：人脸隐私、数据最小化与人工标注

### 修改原则

人脸裁剪图、描述符、底库和考勤均为高敏感数据。默认不通过网络传输描述符，不向无关教师广播摄像头画面，也不允许远程客户端直接写入图库。

### 修改步骤

1. 将人脸识别与底库写入限定在教室端本机管理界面；教师端仅接收经授权的、最小化的出勤汇总（例如学生 ID、状态、更新时间）。
2. 移除 `face-detections` 的全量广播，改为按权限、按需拉取的低频摘要；教师端不应接收 `cropBase64` 和 `descriptor`。
3. 人工标注改为教室端受密码保护的本地操作。若业务确需远程标注，服务端必须再次验证“人脸管理”独立权限、请求关联当前检测帧、短时有效、一次性使用，并记录审计事件。
4. 为图库、数据库和备份启用操作系统级目录权限；评估使用平台密钥链或加密数据库保存人脸模板。
5. 增加数据保留策略：原始截图不落盘；考勤和自适应特征设置明确的保留时长、管理员导出/删除机制和删除审计。
6. 禁止把真实人脸、SQLite 数据库、调试日志、模型转换临时文件提交到仓库。

### 验收标准

- 抓包或协议测试中，普通教师连接无法获得人脸图片、描述符和图库内容。
- 未获“人脸管理”权限的请求无法注册、标注、删除或调整阈值。
- 删除学生时能同时清除图库、考勤和关联缓存，并产生可审计记录。

## 7. 阶段 4：数据一致性与运行稳定性

### 修改步骤

1. 将 `saveData` 的“全表 DELETE 后重写”改为按实体增量 UPSERT/DELETE，并对学生、作业、提交状态使用外键和数据校验。
2. 所有写操作携带 `revision` 或 `updatedAt`；服务端拒绝陈旧版本，返回冲突数据，让客户端提示用户刷新或合并。
3. 删除重复的 `broadcastSync` 调用：`saveData` 完成后只通过一个统一出口通知渲染窗口和已认证会话。
4. 给 `WebSocket.Server` 添加 `error` 监听、端口占用的友好提示及可重试/修改端口入口，避免未处理错误导致进程退出。
5. 为呼叫队列设最大长度、过期时间和去重规则；呼叫端断开后清理 `callMap`、队列内关联项和未确认状态。
6. `ack` 应包含请求 ID 和服务端状态；教师端断线重连后可查询近期呼叫状态，而非永久停留在“已发送”。
7. 密码改用 Argon2id（或 Node 可用的 scrypt）+ 随机盐；增加最小长度、失败退避和本机管理会话超时。作业看板是否需要密码由产品明确决定；若包含个人作业状态，默认应保护。

### 验收标准

- 两个管理端并发修改同一作业不会静默覆盖。
- 端口被占用、网络断开、弹窗关闭、数据库写入失败时应用不崩溃且用户能看到恢复路径。
- 呼叫、数据修改和人脸管理均有可查询的审计记录。

## 8. 阶段 5：人脸识别性能与识别质量

### 修改步骤

1. 设定产品指标：摄像头分辨率、最大同时人脸数、目标帧率、CPU/GPU 占用、最大端到端延迟、误识率与漏识率。
2. 使用单飞检测：上一帧推理尚未完成时不启动下一次推理；不得让 IPC 和 WebSocket 请求无限堆积。
3. 仅在状态变化、固定低频周期或管理页主动刷新时更新出勤摘要；图像/特征绝不按检测帧全量发送。
4. 自适应入库需要二次确认：连续多帧一致、达到更高阈值、同一学生有冷却时间，并支持管理员查看和撤销。避免一次误识污染底库。
5. 原生和 JS 两条识别链路若都保留，使用同一套阈值标定与样本集；不得假设模型转换后的输出格式或预处理与 face-api.js 一致。
6. 将诊断日志采样和滚动写入，限制文件大小与保留数量，避免长期运行占满磁盘。

### 验收标准

- 在标准样本集上报告识别准确率、误识率、漏识率和耗时，指标达到预先批准的阈值。
- 连续运行 8 小时无请求积压、内存持续增长或日志无限膨胀。
- 人工模拟误识时，不会自动向自适应底库永久写入错误模板。

## 9. 阶段 6：测试、CI 和发布

### 测试分层

| 层级 | 重点 | 最低要求 |
| --- | --- | --- |
| 单元测试 | 权限矩阵、密码哈希、数据校验、考勤日期、图库序列化 | 每个安全规则至少一正一反用例 |
| 协议测试 | 认证、令牌过期、越权消息、超大消息、断线重连 | 使用 `ws` 测试客户端自动执行 |
| 集成测试 | SQLite 迁移、呼叫队列、同步、窗口 IPC | 临时数据目录运行，不触及用户数据 |
| 原生测试（如采用方案 B） | 编译、加载、模型、检测、描述符一致性 | 缺少插件/模型即失败 |
| 打包冒烟测试 | 安装后资源路径、模型、动态库、启动 | 三个平台各执行一次 |

### CI 修改

1. 使用 `npm ci`；缓存 npm 与受校验的 ONNX Runtime/模型制品。
2. 先运行 `npm run verify`，成功后才运行 `electron-builder`。
3. 对 Windows/macOS/Linux 分别构建；发布包命名包含版本、平台和架构。
4. 生成 SBOM 或至少保存精确依赖清单；定期在可联网环境运行 `npm audit`，对高危漏洞设置阻断策略。
5. 仅在所有测试通过后上传安装包；失败日志要保留，但必须过滤敏感数据。

### 文档修改

- README 更新 Electron 实际版本、支持平台、安装/构建命令、网络部署条件和端口配置。
- 新增《教师接入与审批》《数据与人脸隐私说明》《故障排查》《发布清单》。
- 删除或标记过期的“原生加速已可用”描述；文档必须与实际发布包功能一致。

## 10. 推荐实施排期

| 迭代 | 内容 | 交付物 |
| --- | --- | --- |
| 第 1 次 | 基线、原生方案决策、构建修复或下线、CI 最小验证 | 可重复构建的安装包 |
| 第 2 次 | 认证、授权、旧协议下线、WebSocket 限流 | 权限测试和迁移说明 |
| 第 3 次 | 人脸最小化传输、本地标注、数据保留 | 隐私验收报告 |
| 第 4 次 | 数据并发、服务异常、呼叫可靠性 | 集成测试与故障演练记录 |
| 第 5 次 | 性能优化、质量标定、跨平台打包验证 | 性能/准确率基准和发布候选包 |

## 11. 上线前阻断清单

以下任一项未满足，禁止将版本部署到真实班级环境：

- [ ] 未认证连接不能看到学生、作业、考勤或人脸数据。
- [ ] 旧版“无身份即班主任”兼容逻辑已移除。
- [ ] 人脸图像和描述符不通过普通 WebSocket 广播。
- [ ] Windows 生产安装包能在干净机器构建、安装、启动和连接；若声明支持 macOS/Linux，对应平台同样完成验证。
- [ ] 原生引擎若随包发布，模型、插件和动态库已在目标平台实际加载并通过测试；否则 UI 和文档明确显示使用 JS 引擎。
- [ ] 数据迁移、备份和恢复演练通过。
- [ ] 权限、协议、数据库和打包测试均在 CI 通过。
- [ ] README、隐私说明和运维文档与成品一致。

## 12. 需要负责人确认的决策

1. 近期选择“暂停原生加速”还是“完整交付原生加速”。
2. 教师端是否允许跨网段访问；若允许，TLS 证书与设备注册由谁管理。
3. 哪些角色能查看考勤、图片、人脸标注和作业看板。
4. 人脸模板、考勤记录、审计日志的保留周期与删除审批流程。
5. 是否需要兼容旧教师端；若需要，迁移截止日期和人工审批策略。
